import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    userImageContainer: {
        borderColor: "#f35bac",
        borderWidth: 1,
        padding: 3,
    },
})

export default styles